# Web-Backend (läuft auf allen Plattformen)
flask>=2.3.0
flask-cors>=4.0.0

# OLED-Display (Waveshare 2.42" SSD1309, SPI)
# Wird bei fehlendem Display sicher übersprungen (Warnung im Log).
luma.oled>=3.12.0
Pillow>=10.0.0

# ANO-Rotary-Encoder (direktes GPIO, nur Raspberry Pi)
# gpiozero ist auf Raspberry Pi OS vorinstalliert.
# lgpio wird als GPIO-Backend für Pi 5 benötigt – Installation via apt:
#   sudo apt-get install -y python3-lgpio
# (lgpio hier absichtlich nicht in pip-Abhängigkeiten, da Build aus Quellcode fehlschlägt)
gpiozero>=2.0

# MQTT-Client (optional – nur aktiv wenn mqtt_enabled=true in settings.json)
paho-mqtt>=1.6.1

# SPI-ADC (AnoPi Shield) – nur auf Raspberry Pi verfügbar
# Auf anderen Systemen schlägt der Import fehl; sensors.py behandelt das
# als "Hardware nicht erkannt" und gibt einen klaren Hinweis im Log aus.
spidev
